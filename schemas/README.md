# Schemas

Machine-readable JSON Schema for the Agent 1 → Agent 2 export contract is
generated into this directory (`docs/06_export_format.md`), for example
`agent1_export_v0.1.schema.json`.

No schema file exists yet; the export layer is implemented in a later phase.
