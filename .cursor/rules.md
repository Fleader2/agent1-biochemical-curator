This repository builds Agent 1 for a virtual microbial cell project.

Always follow PROJECT_SPEC.md.

Never invent scientific information.

Always use type hints.

Use SQLAlchemy 2.0.

Use Pydantic.

Use pytest.

Use Ruff.

Prefer small modules.

Every external connector must be isolated.

Every database model must have unit tests.

Never change the database schema without updating Alembic migrations.

Never write placeholder biology.

Document all public functions.
